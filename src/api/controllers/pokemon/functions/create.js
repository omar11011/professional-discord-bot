module.exports = props => {
    return {
        uuid: props.uuid,
        pokedex: props.pokedex,
        name: props.name,
        region: props.region,
        type: props.type,
        increase: props.increase,
        eggGroup: props.eggGroup,
        gender: props.gender,
        capture: props.capture,
        friendship: props.friendship,
        stats: props.stats,
        movements: props.movements,
        evolutions: props.evolutions,
        category: props.category,
        spawn: props.spawn,
    }
}